import { UserSettingsModelProjection } from 'src/schemas/user-settings.schema';

export const USER_SETTINGS_PROJECTION: UserSettingsModelProjection = {
  darkMode: true,
  receiveNotifications: true,
};
